name: TP1.2

on: 
  push:
    branches: [master]
    
  pull_request:
    branches: [master]
    
jobs:
  build:
  
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v2
      name: prueba tp 1
    - name: Run a one-line script
      run: echo ¡Prueba TP1 DevOps!
